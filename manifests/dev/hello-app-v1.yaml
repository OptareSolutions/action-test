apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-app-v1
spec:
  replicas: 1
  selector:
    matchLabels:
      role: hello-app-v1
  template:
    metadata:
      labels:
        role: hello-app-v1
    spec:
      securityContext:
        # runAsNonRoot: true
        runAsUser: 0
      containers:
      - name: hello-app
        image: gcr.io/google-samples/hello-app:1.0
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"

---
kind: Service
apiVersion: v1
metadata:
  name: hello-v1-svc
spec:
  selector:
    app: hello-app-v1
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080